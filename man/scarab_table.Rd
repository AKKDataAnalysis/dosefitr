% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scarab_table.R
\name{scarab_table}
\alias{scarab_table}
\title{Generate a formatted Scarab table from experimental data}
\usage{
scarab_table(
  results_list,
  drc_results_list,
  plate_name = "plate_01",
  date = "260303",
  experimenter_abbrev = "JD",
  sgc_compound_id = NA,
  tracer_kd_app = NA,
  tracer_concentration_used = NA,
  nLuc_orientation = "C",
  experiment_type = "intact mode",
  cell_line = "HEK293T",
  transfection_mode = "Flask Transfection",
  tracer = "Tracer K10",
  tracer_comment = "at Tracer KDapp",
  assay_volume = "10",
  plate_format = "384-well",
  plate_manufacturer = "784075",
  plate_material = "PS",
  eln_id = NA,
  comments = NA,
  eubscarab_ready = "No",
  fit_used = "log(inhibitor) vs. response (three parameters)",
  save = TRUE,
  file_name = NULL,
  decimal_separator = "."
)
}
\arguments{
\item{results_list}{List containing raw experimental results from multiple plates,
typically generated by the \code{\link{batch_ratio_analysis}} function.
Must contain plate-specific data with modified ratio tables and quality metrics.}

\item{drc_results_list}{List containing dose-response curve fitting results,
typically generated by the \code{\link{batch_drc_analysis}} function.
Must contain final summary tables for each plate.}

\item{plate_name}{Character string specifying which plate to process.
Should match a plate name in both results_list and drc_results_list
(e.g., "plate_01", "plate_02"). Default is "plate_01".}

\item{date}{Character string with experiment date in format "YYMMDD".
Used to construct Experiment IDs. Default is "260303".}

\item{experimenter_abbrev}{Character string with experimenter initials.
Used to construct Experiment IDs. Default is "JD".}

\item{sgc_compound_id}{Character string or NA with SGC global compound identifier/batch number.
This value will be applied to all compounds in the plate. Default is NA.}

\item{tracer_kd_app}{Tracer KD app value as log. Can be specified in three ways:
\itemize{
  \item \strong{Single value}: e.g., \code{-7.5} - applies the same value to all kinases
  \item \strong{Named vector}: e.g., \code{c(LRRK2 = -7.5, AAK1 = -8.0)} - specifies value for specific kinases
  \item \strong{Unnamed vector}: e.g., \code{c(-7.5, -8.0)} - applies values in the order of unique kinases appearing in the plate
}
Kinases without a specified value default to \code{NA} (with a warning).
Default is \code{NA}.}

\item{tracer_concentration_used}{Tracer concentration used as log. Can be specified in three ways:
\itemize{
  \item \strong{Single value}: e.g., \code{-7.0} - applies the same value to all kinases
  \item \strong{Named vector}: e.g., \code{c(LRRK2 = -7.0, AAK1 = -7.5)} - specifies value for specific kinases
  \item \strong{Unnamed vector}: e.g., \code{c(-7.0, -7.5)} - applies values in the order of unique kinases appearing in the plate
}
Kinases without a specified value default to \code{NA} (with a warning).
Default is \code{NA}.}

\item{nLuc_orientation}{Orientation of nLuc. Can be specified in three ways:
\itemize{
  \item \strong{Single character}: "C" or "N" - applies the same orientation to all kinases
  \item \strong{Named vector}: e.g., \code{c(LRRK2 = "C", AAK1 = "N")} - specifies orientation for specific kinases
  \item \strong{Unnamed vector}: e.g., \code{c("C", "N", "C")} - applies orientations in the order of unique kinases appearing in the plate
}
Kinases without specified orientation default to "C" (with a warning).
Default is "C".}

\item{experiment_type}{Character string describing experiment type.
Default is "intact mode".}

\item{cell_line}{Character string with cell line used.
Default is "HEK293T".}

\item{transfection_mode}{Character string with transfection method.
Default is "Flask Transfection".}

\item{tracer}{Tracer name. Can be specified in three ways:
\itemize{
  \item \strong{Single character}: e.g., \code{"Tracer K10"} - applies the same tracer to all kinases
  \item \strong{Named vector}: e.g., \code{c(LRRK2 = "Tracer K10", AAK1 = "Tracer 236")} - specifies tracer for specific kinases
  \item \strong{Unnamed vector}: e.g., \code{c("Tracer K10", "Tracer 236")} - applies tracers in the order of unique kinases appearing in the plate
}
Kinases without a specified tracer default to \code{"Tracer K10"} (with a warning).
Default is \code{"Tracer K10"}.}

\item{tracer_comment}{Character string with tracer comments.
Default is "at Tracer KDapp".}

\item{assay_volume}{Character string with assay volume in uL.
Default is "10".}

\item{plate_format}{Character string with plate format.
Default is "384-well".}

\item{plate_manufacturer}{Character string with plate manufacturer/catalog number.
Default is "784075".}

\item{plate_material}{Character string with plate material.
Default is "PS" (polystyrene).}

\item{eln_id}{Character string or NA with Electronic Lab Notebook ID.
Default is NA.}

\item{comments}{Character string or NA with additional comments.
Default is NA.}

\item{eubscarab_ready}{Character string or named/unnamed character vector
indicating whether the record is EUbScarab ready.  Follows the same
three-way resolution as \code{nLuc_orientation}: a single value applies
to all compounds, a named vector keys by compound name, and an unnamed
vector is applied in compound order.  Accepted values are \code{"Yes"}
and \code{"No"} (default \code{"No"}).}

\item{fit_used}{Character string describing the fitting model used.
Default is "log(inhibitor) vs. response (three parameters)".}

\item{save}{Logical indicating whether to save the table as Excel file.
Default is TRUE.}

\item{file_name}{Character string or NULL for custom file name.
If NULL, generates "scarab_table_plate_name.xlsx" (e.g., "scarab_table_plate_01.xlsx").
Default is NULL.}

\item{decimal_separator}{Character specifying decimal separator for numeric values.
Must be either "." (dot) or "," (comma). Useful for different regional Excel settings.
Default is ".".}
}
\value{
A data frame with 77 rows and multiple columns (one per unique compound).
  The first column "Field" contains the parameter names, and subsequent columns
  (X4, X5, ...) contain data for each compound. The number of compound columns
  depends on how many unique compounds are present in the plate.
}
\description{
This function creates a comprehensive summary table in the Scarab format,
combining data from raw results and dose-response curve fitting. The table
includes experimental metadata, quality control metrics, curve parameters,
and raw BRET values for each compound.
}
\details{
This function is designed to work seamlessly with the output of two other functions
in this package:
\itemize{
  \item \code{\link{batch_ratio_analysis}}: Generates the \code{results_list} with raw data
  \item \code{\link{batch_drc_analysis}}: Generates the \code{drc_results_list} with curve fitting results
}

The function generates a comprehensive table with the following sections:

\strong{Basic Information (Rows 1-10):}
\itemize{
  \item \code{Kinase}: Extracted from column names (e.g., "LRRK2" from "LRRK2:MDKM34")
  \item \code{nLuc Orientation}: Specified via \code{nLuc_orientation} parameter
  \item \code{Compound}: Compound name (e.g., "MDKM34")
  \item \code{Experiment ID}: Generated as \code{date-experimenter_abbrev-Kinase-Compound}
  \item \code{Construct ID}: Auto-generated as \code{{Kinase}A-nb001} (for N) or \code{{Kinase}A-nb002} (for C)
}

\strong{Curve Parameters (Rows 11-18):}
Extracted from \code{final_summary_table} in \code{drc_results_list}
\itemize{
  \item LogIC50, IC50, HillSlope, R2, etc.
}

\strong{Quality Control (Rows 19-29):}
Extracted from \code{interval_means} in \code{results_list}
\itemize{
  \item Controls (Positive, Background), Z', Assay window, etc.
}

\strong{Experimental Details (Rows 30-37):}
\itemize{
  \item Tracer information, plate specifications, etc.
}

\strong{Concentration Data (Rows 38-73):}
\itemize{
  \item \code{LOG Compound Concentration 1-12}: From \code{modified_ratio_table}
  \item \code{Compound Concentration X BRET}: From main columns (without .2 suffix)
  \item \code{Compound Concentration X BRET-Duplicate}: From duplicate columns (with .2 suffix)
}

\strong{Metadata (Rows 74-76):}
\itemize{
  \item ELN ID, Comments, Is EUbScarab Ready?
}

The Construct ID (row 7) is automatically generated based on kinase and orientation:
\itemize{
  \item If orientation is \code{"N"}: \code{{kinase}A-nb001}
  \item If orientation is \code{"C"}: \code{{kinase}A-nb002}
}

All numeric values are converted to character strings to maintain formatting
and allow mixed content in columns. The \code{decimal_separator} parameter
controls whether numbers use "." (e.g., "3.14") or "," (e.g., "3,14").
}
\examples{
stopifnot(requireNamespace("dosefitr", quietly = TRUE))
\donttest{
extdata_dir <- system.file("extdata", package = "dosefitr")
work_dir    <- file.path(tempdir(), "dosefitr_ex_scarab_n")
dir.create(work_dir, showWarnings = FALSE, recursive = TRUE)
invisible(file.copy(
  list.files(extdata_dir, pattern = "^nanobret_", full.names = TRUE),
  work_dir, overwrite = TRUE
))

ratio_res <- batch_ratio_analysis(
  directory        = work_dir,
  info_file        = "nanobret_info.xlsx",
  data_pattern     = "nanobret_plate_\\\\d+\\\\.xlsx$",
  control_0perc    = "1",
  control_100perc  = "24",
  selected_columns = 1:24,
  generate_reports = FALSE,
  output_dir       = tempdir(),
  verbose          = FALSE
)

drc_res <- batch_drc_analysis(
  batch_results = ratio_res, model = "4pl", normalize = FALSE,
  generate_reports = FALSE, output_dir = tempdir(), verbose = FALSE
)

outfile <- tempfile(fileext = ".xlsx")
sc <- scarab_table(
  results_list      = ratio_res,
  drc_results_list  = drc_res,
  plate_name        = "plate_01",
  save              = TRUE,
  file_name         = outfile,
  decimal_separator = "."
)
file.exists(outfile)
}
}
\seealso{
\code{\link{batch_ratio_analysis}} for generating the input results_list,
\code{\link{batch_drc_analysis}} for generating the input drc_results_list.
}
